import { FiltertiendaPipe } from './filtertienda.pipe';

describe('FilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltertiendaPipe();
    expect(pipe).toBeTruthy();
  });
});
